// Tailwind CSS v4: use the separate PostCSS plugin package @tailwindcss/postcss
// (The old 'tailwindcss' PostCSS plugin entry is no longer valid.)
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
